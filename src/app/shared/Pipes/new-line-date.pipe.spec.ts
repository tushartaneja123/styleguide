import { NewLineDatePipe } from './new-line-date.pipe';

describe('NewLineDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NewLineDatePipe();
    expect(pipe).toBeTruthy();
  });
});
