import { HindiTextValidatorDirective } from './hindi-text-validator.directive';

describe('HindiTextValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HindiTextValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
